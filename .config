#!/bin/bash

# Bad Configuration file

# Where is dir with books
# should be one 
DESTINATION="$HOME/Projects/sand_box"

# Where is dir from new have downloaded books
# can be more than one, separate by ':'
GATHER_POINTS="$HOME/Downloads:$HOME/Apps/Tor/tor-browser_en-US/Browser/Downloads"

# What is types of files is transfer from
# gather points to buffer
# can be more than one, separate by ':'
FORMAT="pdf:fb2:epub"

# Where is terminated of transfer 
# should be one
BUFFER="$DESTINATION/no_tag"

echo $DESTINATION $GATHER_POINTS $FORMAT $BUFFER
